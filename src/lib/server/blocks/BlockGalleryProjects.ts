import { client } from "$lib/directus"
import { readItem } from "@directus/sdk"
import { loadBlock, loadCollection, type Collections } from "./Blocks";

const BLOCK_NAME = 'block_gallery_projects';

export const loadBlockGalleryProjects = async ( id: number ) => {
    
}